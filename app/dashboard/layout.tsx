export const metadata = {
  title: 'Dashboard — Tokko',
  description: 'Manage your prompt compressions.',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
